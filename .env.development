# .env para backend (guardar como 'backend/.env')
NODE_ENV=development
PORT=3000
JWT_SECRET=tu-clave-secreta-local
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=authdb
DB_PORT=5432
DB_SSL=false

# .env para frontend (guardar como 'frontend/.env')
REACT_APP_API_URL=http://localhost:3000

# .env.production para frontend (guardar como 'frontend/.env.production')
# Este archivo será utilizado automáticamente en producción
# No es necesario configurarlo ahora, Render lo configurará automáticamente
REACT_APP_API_URL=https://tu-backend.onrender.com